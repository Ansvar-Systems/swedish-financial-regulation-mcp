apiVersion: v1
kind: Service
metadata:
  name: {{ include "swedish-financial-regulation-mcp.fullname" . }}
  labels:
    {{- include "swedish-financial-regulation-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      appProtocol: agentgateway.dev/mcp
  selector:
    {{- include "swedish-financial-regulation-mcp.selectorLabels" . | nindent 4 }}
